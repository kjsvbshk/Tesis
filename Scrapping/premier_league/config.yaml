# Neon Database Connection
# IMPORTANT: This file contains sensitive credentials and should NOT be committed to version control
# Use environment variables or a secure secret management system
# For local development, copy config.yaml.example to config.yaml and fill in your credentials
DATABASE_URL: ${DATABASE_URL}
ESPN_BASE_URL: https://www.espn.com/soccer/
SCRAPE_INTERVAL_DAYS: 1
SAVE_PATH_RAW: data/raw/
SAVE_PATH_PROCESSED: data/processed/
DB_SCHEMA: premier_league

